# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.
If it has to been modified, apply the same changes to AGENTS.md.

## Repository state

**Runtime binding proven, all 37 linkable IMAS-Core C exports forwarded; the data-entry, global-action, slice-action and time-range-action seams discover and register, and ordinary root reads now translate field/timebase pairs.** The build system is verified end to end, and `src/core_binding.rs` / `src/dl.rs` prove the runtime-binding architecture (see `docs/adr/0001-runtime-binding-not-linking.md`): the shim opens IMAS-Core with local symbol visibility via hand-rolled `dlopen`/`dlsym` bindings, checks `getALVersion()` against the version it was built against, and forwards the call — verified by `tests/runtime_binding_test.c` against a recording stub (`tests/stub/`) standing in for IMAS-Core and against a real, CMake-acquired IMAS-Core. `al_context_info`, the utility/version accessors, the thirteen data-entry/action-lifecycle/data-operation symbols, and the seventeen plugin registration, binding, metadata, parameter-setter and reentry symbols forward unchanged this way. The 38th public header declaration, `al_plugin_begin_timerange_action`, is deliberately absent because it is unlinkable upstream; `al_begin_array_struct_action` is not an IMAS-Core symbol (the real name is `al_begin_arraystruct_action`). The export list is compared mechanically with IMAS-Core's. `src/conversion_map.rs` parses a caller-supplied hand-authored equilibrium 3.39.0 ⇄ 4.1.1 conversion-map artifact (`docs/3.39.0--4.1.1.xml`, ADR 0004) and resolves the document-level identity default, `renamed` path-level rules, and `merged`/`split` rules (the latter two as an ordered `CandidatePath` read plan on their ambiguous side, per ADR 0006) to a `RuleExplanation`; `moved`, `retyped`, `left_only` and `right_only` parse structurally but are not yet matched. `src/read_outcome.rs` is the one classifier turning `al_status_t` plus a returned data pointer into failure/not-found/data (ADR 0012); `src/version_stamp.rs` uses it to read and decode `ids_properties/version_put/data_dictionary` (CHAR_DATA, `dim == 1`, sized rather than NUL-scanned) immediately after `al_begin_global_action` opens. `al_begin_dataentry_action` registers its pulse in the context registry (`src/context_registry.rs`, ADR 0003) on success; `al_begin_global_action`, `al_begin_slice_action` and `al_begin_timerange_action` all register a root conversion record through the one shared `discover_and_register_occurrence` rule (`src/resolve.rs`, issue #55) only when a present, valid stamp names a stored DD version that both differs from the latched HLI DD version (ADR 0005) and has an embedded artifact to serve it (`src/known_artifacts.rs`, the one equilibrium 3.39.0⇄4.1.1 artifact until a generator exists) — a matching or absent stamp registers nothing (ADR 0007), and a malformed present stamp refuses and ends the just-opened context rather than leaking it (ADR 0009). The IDS name is forwarded unchanged at every one of these seams. `datapath` on `al_begin_global_action` is translated only once a prior open of the same occurrence already cached a mismatch; on first use it forwards unchanged, since the version that would justify translating it is not yet known at the point IMAS-Core must be called — slice and time-range actions carry no `datapath` argument, so only the discovery/registration half of the rule applies to them. `al_end_action` removes only its own context's record on success. `al_begin_arraystruct_action` resolves its `path` and `timebase` through its parent record before IMAS-Core is called and, on success, registers the returned context ID as a child conversion record inheriting the shared map, root identity, and stored-direction (issues #54 and #61); `al_read_data` resolves each nonempty `field` and `timebase` independently through that map before IMAS-Core is called (issue #56). Explicit identity and renamed rules reach their stored spelling, no-source returns success with a null data pointer without a Core call, and relative/absolute arguments resolve from the context/IDS roots respectively; matching, unknown, unstamped, and conversion-disabled contexts remain plain forwards. The scalar tracer uses HLI-owned storage as IMAS-Core's scalar ABI requires; the shim preserves that pointer unchanged. Still unimplemented: reading a `merged`/`split` candidate plan (issue #57), executing a value transformation such as a COCOS sign flip (issue #59).

**Update:** `al_read_data` now executes `merged`/`split` read plans in declared candidate order in either stored-direction, classifies each result through the shared read-outcome rule, and applies the selected COCOS sign flip in place while retaining its loss record. AOS container path translation remains limited to concrete, untransformed paths.

**Update (issue #65):** every non-exact successful read — a single translated `renamed`/`moved` path as well as a `merged`/`split` candidate plan — now reaches its root context's loss log (`src/context_registry.rs`, ADR 0012); a fast-path short-circuit that previously skipped logging for the single-path case is gone. Two new shim-owned exports, `imas_mvdd_context_loss_count` and `imas_mvdd_context_loss_at` (`tests/owned_exports.def`), let a caller drain that log without allocating: a query on a child context resolves to its root, an untracked context reports zero rather than a refusal, and `loss_at` copies one path and its fidelity verdict into caller storage, refusing safely on a null output, a negative index/length, an out-of-range index, or a buffer too small for the path.

**Update (issue #62):** `al_read_data` through a live arraystruct context — opened one or more levels deep by `al_begin_arraystruct_action` — was already generic over root and child contexts (`resolve::resolve_read_path` anchors every relative argument on `ConversionRecord::resolved_path`, whichever context that record belongs to, and `resolve::stored_anchor` translates a *renamed* child anchor before stripping it back off a resolved stored path); this issue proves it rather than changing it. `tests/nested_context_read_test.c` covers the one path no earlier issue exercised — a relative field/timebase resolving beneath a child whose own anchor is itself a `renamed` rule, not just an identically-spelled one like `time_slice` — plus anchor-independent absolute resolution, no-source, refusal, and a COCOS sign flip, all addressed relative to a live child context. `tests/equilibrium_read_test.c` adds the matching real-Core fixture proof: `constraints/bpol_probe`/`constraints/b_field_pol_probe` (renamed, literal `0.42`) and `constraints/flux_loop` (identically spelled, COCOS-flipped `measured`, literal `1.15`/`-1.15`), both nested beneath `time_slice`. The remaining two acceptance criteria are architectural rather than ABI-observable and are discharged by inspection, not a new test: the registry lock is already proven to release before any conversion work in `src/context_registry.rs`'s `child_lookup_releases_the_lock_before_returning` (issue #60), and `ContextRegistry`'s public interface has no ancestry-walking method at all — a child record resolves its root identity, pulse context ID, and shared conversion map from a live parent *snapshot* taken once at `al_begin_arraystruct_action` time, never at read time.

**Update (issue #66):** `#65`'s registry-level root resolution (`ConversionRecord::root_id`, inherited once by a child at `record_child` time) already attributed a nested loss entry to the right root; what it recorded there was wrong. Every `retain_read_fidelity`/`retain_read_fidelities` call site in `src/resolve.rs::read_data` retained the raw, unjoined `field`/`timebase` argument rather than that argument joined onto `record.resolved_path` — invisible at the root, where `resolved_path` is always empty, but under a live arraystruct child it silently dropped the anchor prefix (e.g. `"boundary_separatrix/gap/r"` instead of `"time_slice/boundary_separatrix/gap/r"`), unlike the refusal path (`read_refusal`), which already used the joined `dd_path`/`field_dd_path`. Both helpers now take the live `ConversionRecord` and join with `resolve::join_hli_path` before calling `ContextRegistry::record_read_loss`, matching the path a refusal already reported. `tests/nested_context_read_test.c` proves the complete DD path is retained for a certainly-lossy (`moved`) and a potentially-lossy (`merged`) nested read, that a query on the live child and on the root resolve to the same entry, that an exact/passthrough nested read adds nothing, and that the log dies with the root even when a child closes non-LIFO; `src/context_registry.rs` gains a matching direct unit test for the non-LIFO case. No sibling scan or hierarchy walk was ever needed — the registry already resolves child→root in one field lookup — so this issue is a one-line path-construction fix plus its proof, not an architecture change.
**Update (issue #63):** `al_end_action`, `al_iterate_over_arraystruct`, and `al_close_pulse` already had the registry behavior these acceptance criteria describe — non-LIFO close, recycled context IDs, and plain forwarding for the latter two were never a distinct code path, only unproven at the public ABI seam. `tests/context_lifecycle_test.c` (new) proves each one the same indirect way `version_discovery_test.c` and `nested_context_read_test.c` already do — there is no C-level registry introspection, so a later `al_read_data` through a still-live context either translates (record intact) or forwards unchanged (record gone/never existed): ending a child leaves its parent's record live and vice versa, a refused close (`RECORDING_STUB_END_ACTION_FAIL`, new stub knob) leaves the record intact, a raw context ID recycled onto a matching-version reopen never exposes the ended occurrence's stale record, `al_iterate_over_arraystruct` forwards `aosctx`/`step` unchanged without touching the registry, and `al_close_pulse` forwards `pulseCtx`/`mode` unchanged without clearing the pulse's occurrence-version cache. Concurrent close/read safety on one IMAS-Core context was already out of scope by design (ADR 0003).
**Update (issue #64):** `al_write_data`, `al_delete_data`, and the matching plugin reentry `al_plugin_write_data` now refuse before IMAS-Core is called whenever `ctx_id` carries a live conversion record — a known mismatched root or a child context that inherited one — per `docs/adr/0002-read-path-seam-policy.md`'s stated rule ("If known versions differ, return failure without calling IMAS-Core"). The refusal is a blanket, context-keyed check (`ContextRegistry::lookup`) that never resolves `field`/`timebase`/`path` through the conversion map, so write-path translation remains unintroduced. Matching, unknown, unstamped, and conversion-disabled contexts carry no record and forward unchanged, as before. `tests/write_delete_conversion_test.c` covers direct write/delete refusal under a known mismatched root and through a nested arraystruct child, the plugin-write refusal, plus the four forwarding cases, and confirms the caller's `data`/`size` storage is left untouched by a refusal.
**Update (issue #67):** `al_plugin_begin_global_action`, `al_plugin_begin_slice_action`, `al_plugin_begin_arraystruct_action` and `al_plugin_end_action` now carry the same context creation, path translation, and lifecycle policy as their non-`al_plugin_` twins, rather than forwarding verbatim. The shared policy itself is factored into three new `*_impl` helpers in `src/resolve.rs` — `begin_global_action_impl`, `begin_slice_action_impl`, `begin_arraystruct_action_impl` — each taking a `forward` closure that calls the real ABI symbol (`al_begin_*_action` or its `al_plugin_begin_*_action` twin); `begin_global_action_impl` additionally takes an `end_on_refusal` closure so a malformed-stamp refusal ends the just-opened context through the matching family — `al_end_action` for an ordinary open, `al_plugin_end_action` for a plugin reentry open — since a context opened through one family must be closed through that same family (`begin_slice_action_impl` takes the same closure for the same reason). `begin_global_action`/`begin_slice_action`/`begin_arraystruct_action` and their new `plugin_begin_*` twins are now thin wrappers over these three shared functions, differing only in which real symbol and end-action twin their closures forward to; `discover_and_register_occurrence` (`src/resolve.rs`, issue #55) itself needed no further change beyond the `end_on_refusal` parameter it already gained. `plugin_end_action` removes only its own context's record on success, mirroring `end_action` directly (no shared helper — the two bodies are four lines each, matching the codebase's existing tolerance for that scale of twin duplication, e.g. `write_data`/`plugin_write_data`). `al_plugin_read_data`/`al_plugin_write_data`'s read-side policy — value transformation, loss reporting, and read-outcome classification through the plugin seam — is issue #68's scope, not this one; `al_plugin_write_data`'s mismatch refusal already landed under issue #64. `tests/plugin_reentry_policy_test.c` (new) proves discovery/registration/translation/malformed-refusal/failure-forwarding for the plugin global and slice twins, translation/refusal/failure-forwarding for the plugin arraystruct twin (verified, as with the ordinary seam, via a later ordinary `al_read_data` through the same child `ctxID`), and record-removal-on-success/record-retention-on-failure for the plugin end-action twin — the same externally-observable-only-through-a-later-read technique `version_discovery_test.c`/`context_lifecycle_test.c` already use, since there is no C-level registry introspection. Proving this required a stub fix: `tests/stub/recording_stub.c`'s generic `record_plugin_call` recorder retained the raw `first`/`second` string pointers it was handed rather than copying them, which was invisible while every plugin seam forwarded its caller's own stable argument verbatim, but dangled the instant a seam forwarded a translated `CString` the shim frees right after the call returns; it now copies via the same `record_str` helper every other stub recorder already uses. New stub fail-knobs `RECORDING_STUB_PLUGIN_GLOBAL_FAIL`, `RECORDING_STUB_PLUGIN_SLICE_FAIL`, `RECORDING_STUB_PLUGIN_ARRAYSTRUCT_FAIL` and `RECORDING_STUB_PLUGIN_END_ACTION_FAIL` mirror their ordinary-seam counterparts.

**Update (issue #68):** `al_plugin_read_data` now carries exactly the same policy as `al_read_data` — registry lookup, conversion-map resolution (identity, `renamed`, and the `merged`/`split` candidate loop), value transformation (COCOS sign flip), read-outcome classification, and root loss-log retention — rather than forwarding verbatim. Both are thin wrappers over one shared `read_data_impl` in `src/resolve.rs`, parameterized by a `forward` closure that calls through to the real `al_read_data` or `al_plugin_read_data` symbol; nothing else differs between the two seams, so a fix or extension to one automatically reaches the other. `al_plugin_write_data`'s refusal-under-mismatch policy already existed from issue #64 and needed no change. `tests/plugin_reentry_policy_test.c` gained scenarios proving translation-under-mismatch, refusal-before-core, no-source, merged-candidate fallthrough, sign-flip execution, and loss-log retention through a plugin-opened child context, all through the plugin reentry seam; `tests/write_delete_conversion_test.c` gained the previously-untested "matching context forwards unchanged" case for `al_plugin_write_data`. `tests/stub/recording_stub.c`'s `al_read_data` and `al_plugin_read_data` now share one `compute_read_response` helper so the same not-found/sign-flip/size-override fixtures drive both seams identically — previously `al_plugin_read_data` returned a fixed canned response regardless of test setup. The TDD contract's real-IMAS-Core half of this seam was closed shortly after, by PR #93: `tests/real_core_forwarding_test.c`'s `check_plugin_read_conversion_against_real_core` drives a plugin-seam read across a known DD-version mismatch against genuine IMAS-Core, alongside `check_plugin_reentry`'s plain forwarding proof.

**Update (issue #69):** no shim behavior changed here; this closes the read-conversion effort's validation matrix and states its limits where a user reads them. `tests/scoped_passthrough_test.c` (new) is the piece that was missing: with a mismatched equilibrium occurrence open and demonstrably converting (each scenario first proves an ordinary `al_read_data` translates), `al_get_occurrences`, `al_list_filled_paths` and `al_bind_plugin`/`al_unbind_plugin` must still forward unchanged, and so must every remaining non-seam export — the utility/version accessors, the plugin registration/metadata/readback family, and all three parameter setters. Their path arguments are deliberately the two spellings of `rename-beta-normal`, and `al_list_filled_paths`'s returned list is seeded (new stub knob `RECORDING_STUB_FILLED_PATHS_CSV`) with the *stored* spelling, so a shim that began rewriting these would produce a visibly different string rather than pass by coincidence; each assertion was confirmed to fail under a deliberately mutated expectation before being left green. It also pins that `getDDVersion()` keeps returning IMAS-Core's `"!!DEPRECATED!!"` sentinel even when the shim has just discovered that occurrence's stored DD version — the shim owns stamp discovery without repurposing Core's dead accessor. `tests/equilibrium_read_test.c` gained the matrix's missing real-Core cells: refusal outcomes in *both* fixture directions, deliberately picking two paths that refuse for different reasons: `time_slice/constraints/strike_point/chi_squared_r`, which a `redefine` entry declares `unmappable` each way, and `grids_ggd/grid/space/coordinates_type`, the artifact's one `retyped` rule, which is declared *`exact`* each way and refuses anyway because `Rel::Retyped` resolves to `RefusalReason::UnservableRetype` independently of fidelity — the shim cannot reshape an int array into an array of identifier structures, so a conversion that is lossless in principle is unavailable in practice. Both are asserted on status code, exact message including that direction's version pair, untouched caller storage, and an `UNMAPPABLE` loss entry, which is what a refused read is from the caller's side whatever the rule's declared fidelity was, plus the merged direction that was absent — a 3.39.0 HLI resolving `profiles_2d/b_tor` to the 4.1.1 fixture's single folded `b_field_phi`, which also gives the loss-query exports their first real-Core coverage (`POTENTIALLY_LOSSY`, ADR 0008's unverified bucket rather than its certain one, because a `merged` rule's declared `lossy` is deliberately not confirmed by an auxiliary read). `scenario_reverse_merged_read_falls_through_to_stored_alias` was renamed `forward_*`: it latches 4.1.1 against the 3.39.0 fixture, which is this file's *forward* label, and it was the one scenario contradicting the convention its eight neighbours follow. That convention is now stated in the file header, including that it is deliberately the opposite of `conversion_map::Direction` (named after which side supplies the path, so a 3.39.0 HLI travels `Direction::Forward`) — CMakeLists.txt's read_path_test comments use the enum's sense, while this file names the fixture under test. Review of this issue also turned up a defect in the matrix's own oracles, fixed here: eleven call sites across `tests/read_path_test.c`, `tests/runtime_binding_test.c` and this issue's new test passed small ordinals for IMAS-Core data types under a comment naming the right constant — `3 /* DOUBLE_DATA */`, `2 /* INTEGER_DATA */`, `4 /* COMPLEX_DATA */` — where the real values are `DATA_TYPE_0` (50) plus an offset, so `CHAR_DATA`/`INTEGER_DATA`/`DOUBLE_DATA`/`COMPLEX_DATA` are 50/51/52/53 (IMAS-Core's `al_defs.h.in`). The stub-only profile acquires no IMAS-Core and so has no `al_const.h` to include, which is why these are literals at all; `read_path_test.c` was already using the correct `52` in one helper and the wrong `3` in another. Mostly this was cosmetic — a refusal driven by the path never reads the datatype — but `scenario_unsupported_sign_flip_types_refuse_without_core_call` was asserting that "integer and complex reads are refused" while actually passing two codes that name no IMAS type at all, so it demonstrated only that *some* non-`DOUBLE_DATA` value refuses. It now passes 51 and 53. Every suite stays green with the corrected values, which is itself the evidence that the other ten sites were cosmetic. #69's own sweep reached only seven of the eleven: four more sat in `runtime_binding_test.c` itself — the not-found `al_read_data` probe, `al_setvalue_parameter_plugin`'s `int` payload, and both plugin read/write reentry calls with their `first_int()` oracles — and were corrected in a later pass, so the count here is the total across both. A third pass during the read-path review's fix batch found ten more the first two both missed -- seven in `tests/nested_context_read_test.c` and three in `tests/arraystruct_path_test.c`, all `3` for `DOUBLE_DATA` in an `al_read_data` datatype argument -- bringing the real total to twenty-one. Every suite stayed green with the corrected values, so these ten were cosmetic in the same way as the earlier ten; the lesson is about the claim, not the codes. A bare small integer in a datatype argument is the shape to grep for (`&data, 3,`), and the stub-only profile's lack of `al_const.h` is why these are literals at all. `README.md` gained a "Scope and limitations" section: one DD version per process and why the environment fallback makes it a launch-time property, the self-converting-client exclusion, the IMAS-Fortran-first validation scope, the one-IDS/one-version-pair conversion boundary, the three deliberately-untranslated seams, and — stated plainly — that a green suite is not a deployment mechanism, because nothing here places the shim in front of a real HLI or in any HLI's runtime search path. Its status paragraph had still said "no conversion logic yet" and now describes what is actually implemented.

**Update (reentrant reads, ADR 0014):** a read seam entered while this thread is already inside one is now forwarded to IMAS-Core exactly as received — no registry lookup, no conversion-map resolution, no value transformation, no loss retention — for `al_read_data` and `al_plugin_read_data` alike, since both are the same `read_data_impl`. Such a call is not the caller the shim converts for: by the time IMAS-Core has been called the path in flight is already in the stored DD version, so resolving it again translates a stored path as though it were an HLI one. This replaces an address-keyed guard (`READ_POLICY_STATE`, a thread-local `(depth, Option<usize>)` comparing `*data as usize` against the last transformed pointer) that was simultaneously too narrow — it suppressed only the second *sign flip*, leaving the second path resolution and a spurious loss entry in place — and too wide, since an allocator may return a previously-seen address for a different buffer, which would have skipped a *required* flip. `SHIM_READ_DEPTH` plus `ReadDepthGuard` is one counter with no allocator assumptions. The defect was found by pinning the merged-read loss count in `tests/equilibrium_read_test.c`: the log is shim-owned (ADR 0003, ADR 0012), so its entry count is platform-independent by construction, and a Linux real-Core run reporting 2 where macOS reported 1 was therefore a shim bug rather than platform noise — the extra entry was keyed on the *stored* spelling the same read had just translated into, logged from inside the outer read. IMAS-Core's internal call to its own public `al_read_data` binds to the shim's exported definition on ELF but not under macOS's two-level namespace, which is why this only ever appeared in CI. It is now covered platform-independently: `recording_stub_set_reentrant_read` arms the stub to call back into the shim mid-read with a caller-supplied field (a function pointer, not a `dlsym` — the stub exports `al_read_data` itself, so `RTLD_DEFAULT` would find its own definition and recurse), and `tests/read_path_test.c` gains `reentrant-read-is-forwarded-unchanged` (the reentrant path reaches Core verbatim and the root loss log keeps exactly the one entry the caller earned) and `reentrant-read-does-not-reapply-a-sign-flip` (the stub hands both legs the same static buffer, so a second flip would restore the original signs). Both were confirmed to fail against the unfixed shim before being left green, for the two distinct reasons above.

**Update (spec-axis review fixes P1, P3, P5, P13):** four findings from the `feat/path-conversion` code review, each independently confirmed against the code. **P1** — issue #50 AC1's "every DD path is claimed by a rule" cannot fail as written, because the artifact's `<default rel="identical"/>` makes `resolve` match everything, so `CompletenessViolation::UnclaimedInventoryPath` is unreachable for the shipped artifact and its only test builds a default-less map. The default stays (ADR 0004; removing it would change runtime resolution and is a design decision, not a review fix), and instead `check_completeness`'s doc now says which violation is unreachable and which one carries AC1's weight (`DefaultAssumesMissingCounterpart`), with both reachability facts pinned against the *approved* map rather than a toy one. The coverage report splits `supported=` into `by rule=` and `by identity default=` (342 = 72 + 270 forward, 358 = 65 + 293 reverse) so how much of the headline number rests on the default's assumption is visible. Both inventories had also omitted `ids_properties/**` and `code/**` wholesale — including `ids_properties/version_put/data_dictionary`, the one path the shim itself reads at every open — because the imas-dd path enumeration excludes those subtrees and therefore cannot supply them; that one path is now in both inventories (confirmed STR_0D in 3.39.0 and 4.1.1 via `check_dd_paths`), claimed by identity with a verified counterpart, and a test fails if it drops out again. Totals move 427/392 → 428/393. **P3** — `tests/verify_artifact_coverage_floor.cmake` asserted only that the report *contains* `supported=`, a substring ending at the `=`, so `supported=5` passed; its one negative fixture drops every rule at once, and `49/49` was hardcoded. The standards-axis batch (PR #98) had already made each direction's counts parse as numbers and account for the total, while deliberately leaving the floor itself and the near-boundary proof to this finding; both halves are now merged, keeping that batch's literal-dot escaping so a gate cannot match a direction it was not asked about. On top of it, the gate checks each total against the line count of the inventory file that side names, checks that `by rule` plus `by identity default` accounts for `supported`, compares supported coverage against floors pinned in `CMakeLists.txt` (342/358), derives the IMAS-Python baseline total from the TSV, and adds the near-boundary case that was missing — the approved artifact minus exactly one rename rule (`rename-beta-normal`, worth exactly one baseline mapping), generated inside the script so it cannot drift, which must be rejected reporting 48/49. Every new assertion was confirmed red under a mutated expectation, under CMake 3.31.6 (CI's pin) as well as 4.x. **P5** — the `begin_*` seams short-circuit on `hli_version::current()`, but every data-path seam called `REGISTRY.lookup` unconditionally, taking a mutex per `al_read_data` on the path a non-converting HLI takes for every field it reads, against issue #56 AC5 ("conversion-disabled contexts bypass registry lookup and rule resolution"). One new `resolve::live_conversion_record` puts `hli_version::conversion_is_possible()` — the same question `current()` answers, without cloning the version — ahead of the lookup for `read_data_impl`, `write_data`, `delete_data`, `plugin_write_data` and `begin_arraystruct_action_impl`. A record can only exist where a latched HLI version allowed one and the latch is a `OnceLock` that never falls back, so the short-circuit is sound by construction; the *matching* and *unknown* halves of AC5 still cost the one lookup ADR 0003 budgets, since neither is knowable without asking. Proven observably rather than by inspection: no unit test can latch a version, so a test that registers a real record and still gets `None` back could only have been answered by the latch. **P13** — `version_stamp::discover`'s doc claimed it read via "the real IMAS-Core `al_read_data` binding" while calling `resolve::read_data`, the converting wrapper, i.e. reentering the conversion layer from inside the code that produces its input; it forwarded unchanged only because registration happens after discovery, an ordering accident. New `resolve::read_data_unconverted` forwards to the same real symbol with none of `read_data_impl`'s policy, and still enters the read-depth counter so a read arriving from underneath the IMAS-Core call is reentrant by ADR 0014's rule rather than by that accident. ADR 0014's "What this does not change" section described the old arrangement and now describes this one.

**Update (remaining confirmed review findings P2, P4, P6, P7, P9, P10, P12, P14):** the rest of the `feat/path-conversion` review's confirmed findings, each re-verified against the code before being touched, and two of the report's own claims corrected in the process. **P10** — `docs/inventory/equilibrium-4.1.1.txt` listed 23 paths under `time_slice/ggd/grid`, byte-identical to the 23 the 3.39.0 inventory lists, while the artifact's own `drop-timeslice-ggd-grid` rule declares that whole subtree `left_only`: the shipped data contradicted itself. `check_dd_paths` confirms all 23 absent from DD4 and present in DD3, each one's nearest DD4 suggestion being the `grids_ggd/...` spelling. Because they appeared on both sides they matched no right-side rule (`load` indexes a `left_only` rule into `left_sources` alone), fell through `<default rel="identical"/>`, and counted as supported reverse coverage — overstating it by exactly 23, so `REVERSE_SUPPORTED_FLOOR` was pinned over an inflated figure and moves 358 → 335 (`by identity default` falls 293 → 270, matching forward's 270; forward is unaffected). The rule's note saying "37 paths" is deliberately *not* changed: the notes count the raw imas-dd path-set surface the artifact header cites (563 left / 455 right), not the leaf inventories, and every other note is higher the same way — `drop-boundary-secondary-separatrix` says 12 against 8 leaves, `new-contour-tree` 10 against 7. The review read that as a miscount; it is a different surface. `check_completeness` could not have caught any of this, because `DefaultAssumesMissingCounterpart` compares the two inventories *with each other* and a path wrongly present on both satisfies it, so it gains one assertion that closes the class: a `left_only` rule's own selector must match nothing in the right inventory and a `right_only` rule's nothing in the left, reported as `CompletenessViolation::SideOnlyRuleContradictedByInventory`. An absence is only provable against the inventory that denies it. **P2** — issues #50 and #51 name the same test seam, "artifact-validation command invoked through the project test runner", but only #51's floor ran as a command: `check_completeness` had no call site outside `#[cfg(test)]`, so the command could report a healthy summary for an artifact whose claims did not hold. `validate_equilibrium_coverage` now runs it, deliberately *after* the IMAS-Python floor check, because that error is the contract two of the gate's own fixtures assert on and both would otherwise trip a completeness violation instead; the completeness half therefore gets its own near-boundary fixture (the approved artifact minus `drop-b-flux-pol-norm`, generated in the script). **P12** — `resolve_single_path` refused a declared `Fidelity::Unmappable` while `resolve_merged`/`resolve_split` did not, so an `unmappable` fold would have produced a candidate read plan for `al_read_data` to execute. Latent (all thirteen merged rules are lossy/exact, the one split exact/exact, and `load` does not reject the combination) but fixed by hoisting the check into one `refusal_before_resolution` guard ahead of dispatch, so it cannot be a per-kind check a resolver forgets; the guard keeps `retyped`'s precedence, which is now pinned by test rather than implied by arm order. **P14** — user story 47 and #43's own Solution paragraph both require a path no rule claims to return not-found, and `resolve_read_path` forwarded it instead. The cause was one conflated verdict: `claimed_argument` returned `Option`, whose `None` meant both "no usable path" and "no rule claims it", so `resolve_arraystruct_argument` re-derived the difference from the raw pointer afterwards and the read seam did not. It now answers three ways (`ReadArgument::Absent`/`Unclaimed`/`Claimed`), the arraystruct wrapper drops its re-derivation, and an unclaimed read retains an unmappable verdict so the caller can see why it came back empty. Latent behind the identity default, which is also why it is proven by a unit test over a no-default fixture artifact — no C-ABI test can reach the branch. That fixture has to be cached under an IDS name of its own (`equilibrium-no-document-default-fixture`): `record_root` obtains its map through the registry's `(ids, stored, hli)` cache, so keying the fixture on the real `equilibrium` 3.39.0/4.1.1 pair made it share whichever map was already live for that pair — the approved one, whose `<default rel="identical"/>` claims every path — and the test failed on roughly one `cargo test` run in 150, whenever `a_data_path_seam_answers_before_the_registry_when_conversion_is_disabled`'s root record was concurrently alive. A distinct context ID does not help; the map cache is keyed on the version pair, not the context. **P6** — #58 AC3 asks that *every* refusal name reason, DD path, HLI version and stored version; only the read seam complied. The usual justification (issue #64 made write/delete a blanket context-keyed check that resolves no path) does not hold: the record triggering the refusal carries both versions and the caller's own argument is the spelling AC3 wants. One new `contextual_refusal` serves the write, delete, plugin-write and both arraystruct failures, reusing the existing formatter and so its truncation order; `read_conversion_refusal` is renamed `path_conversion_refusal` now that it serves five seams. `tests/shim_test_support.h` gains `CHECK_REFUSAL_MESSAGE`, asserting the contract as one exact string rather than four substring searches. **P7** — two mechanisms have no coverage from the approved artifact at all and both were mis-read during review, so they are now asserted instead of assumed. `RefusalReason::Unmappable` is unreachable: the artifact declares `unmappable` 36 times and every one sits on a `left_only` rule's `reverse` or a `right_only` rule's `forward`, the direction that rule can never be selected in, so sweeping both inventories both ways yields only 1/2 shape refusals and 4 unit redefinitions each way and zero unmappable ones. The glob *stage* is unreachable too — no rule uses a glob selector, and the four `<redefine glob="...">` entries are matched by `redefine_for` against an already-resolved right-side path, not through `best_match`'s stage ladder. Neither is a defect; both bound what a green suite proves, and the new tests fail telling the reader to add real coverage if a future artifact makes either reachable (ADR 0011). `Rel::LeftOnly`/`RightOnly`/`Retyped` gain the doc comments that would have prevented the mis-reading, including that a retype's `shape` attribute is deliberately not parsed because the refusal is unconditional. **P9** — ADR 0002's seam table gave `datapath` a two-valued policy while `translate_down` has three branches; the table now states the concrete-path condition and a new paragraph says why an unservable `datapath` forwards rather than refusing (it is near-inert on five of six backends, so refusing an open over it would deny an occurrence the caller can still read). **P4** — spec #43 defines tier 2 as real-IMAS-Core tests, and three lettered obligations ran against the recording stub only. (c) write-against-mismatch now refuses `al_write_data`/`al_delete_data`/`al_plugin_write_data` on a real mismatched occurrence and then reads through the same context to show the refusals were policy rather than a broken context. (e) the unstamped read had the word "unstamped" in no real-Core test; both fixtures are stamped, so the check copies the 3.39.0 fixture into the temp dir and deletes the stamp from the copy, and proves an asymmetry no single-spelling read could — the stored spelling reaches 1.8 while the HLI's own must not be rewritten into it (confirmed discriminating: leaving the stamp in place trips it). (g) covers iteration, child close and pulse close across a real lifecycle, deliberately leaving parent-before-child to the stub suite because real IMAS-Core owns an ordering the HLI contract does not promise. Real-core-labelled tests 15 → 17, full suite 164 → 166, stub-only 145 unchanged. Also here: the issue-#69 datatype-ordinal sweep, twice claimed complete in this file, was still missing ten sites — seven in `tests/nested_context_read_test.c` and three in `tests/arraystruct_path_test.c` — bringing the real total to 21; all green with the corrected values, so cosmetic like the earlier ten, and the #69 paragraph now records the grep shape (`&data, 3,`) rather than another completeness claim. Two review findings were *not* acted on: **P11** (`time_slice/ggd/grid/space/coordinates_type` "has no rule") is claimed forward by `drop-timeslice-ggd-grid`'s subtree and only reached the identity default in reverse because of P10's bad inventory, so fixing P10 dissolves it and adding a rule for it would encode a path DD4 does not have; **S4** (`Fidelity::Lossy` breaching CONTEXT.md's "avoid" rule) is not a breach, since `PotentiallyLossy` sits alongside it and the XML grammar is three-valued with `PotentiallyLossy` produced at resolve time, leaving only the doc-comment gap that nothing states `IMAS_MVDD_FIDELITY_LOSSY` is the certainly-lossy bucket.

**Update (issue #105, part A of the #101 series):** `src/resolve.rs`'s first 650 lines — the ADR 0001 runtime binding — moved verbatim to `src/core_binding.rs`, the file ADR 0015 already names for it: the 25 `extern "C"` fn-pointer types, the `core_binding!` manifest macro and the `CoreBinding` it generates, `VersionDrift`/`ResolutionError`, the `CORE` `OnceLock` and `resolve()`, `SymbolResolver`, `library_path`/`bare_soname`, the `check_major_version` gate, the `const2str`/`err2str` fallback tables and their constant IDs, `failure()`, the six plain accessors `lib.rs` calls directly (`get_backend_id`, `build_uri_from_legacy_parameters`, `const2str`, `err2str`, `get_al_version`, `get_dd_version`), and the nine unit tests covering `library_path`, `check_major_version` and `failure`. `resolve.rs` keeps every seam and falls 2790 → 2129 lines. No function body changed: the only edits to moved code are visibility (`CoreBinding` and its generated fields, `core()`, `DOUBLE_DATA_ID` and `CHAR_DATA_ID` become `pub(crate)`) and the `forward_status!` macro, which now expands `$crate::core_binding::core()` — `macro_rules!` is not hygienic for paths, so the expansion must name the function from wherever it lands — and is re-exported by `pub(crate) use forward_status;` so `resolve.rs` can `use crate::core_binding::forward_status`. That macro is the only *control* crossing the two files; what else crosses is data — `CHAR_DATA_ID`/`DOUBLE_DATA_ID`, compared against by `version_stamp.rs` and `resolve.rs`'s sign-flip type check, and the six accessors `lib.rs` mirrors straight out. `SHIM_READ_DEPTH`/`ReadDepthGuard` deliberately stayed behind even though they sit inside the moved line range: they enforce ADR 0014, not 0001, and ADR 0015 puts the depth guard in the interposition layer because it gates whether policy runs at all. The C ABI suite is untouched — no assertion added, removed or edited, 166 tests with 17 real-core labelled, all green — which is the whole regression net for a move; `cargo test`'s 161 unit tests are unchanged in number and split 9 to `core_binding`, 8 left in `resolve`. That gate is why three prose comments under `tests/` still say `src/resolve.rs` hand-transcribes `al_const.h`'s id-to-string tables when those tables now live in `core_binding.rs` — `tests/real_core_abi_core_check.c:20` and `:66`, and `tests/abi_fallback_constants.def:1`. The series gate ("not one assertion added, removed or edited") would have permitted a comment-only correction, but AC4 is the stricter of the two and asks for a byte-identical suite, which is also the property a reviewer can check mechanically on a move; part C's rename of `resolve.rs` forces that sweep anyway. Both review axes flagged these independently, so they are deferred deliberately rather than missed. `CMakeLists.txt`'s bare-soname comment and `README.md`'s layout table, which are not the test suite, are corrected here. Renaming `resolve.rs` itself is part C's work, when there is something to rename it to.

**Update (issue #106, part B of the #101 series):** `Outcome` was interpreted at three independent sites in `src/resolve.rs`, each deriving a different subset of its meaning — `translate_down` derived a `CString` or nothing, the read seam derived a fidelity-and-candidates read plan, and the context-opening seams derived one concrete spelling, no-source, or a refusal. New `src/path_conversion.rs` is now the one place that answers *what stored path does this HLI argument mean, and at what fidelity* — it knows nothing about seams, attempts, loops or IMAS-Core. Moved in: `resolve_read_path`, `resolve_context_path`, `claimed_argument`, `ReadArgument`, `ClaimedArgument`, `ContextPathResolution`, `ReadPath`, `TranslatedReadPath`, `ResolvedReadPath`, `ConcreteStoredPath`, `stored_anchor`, `concrete_stored_path`, `translated_read_component`, `stored_c_path`, `read_fidelity`, `translated_read_fidelity`, `join_hli_path`, `strip_anchor`, `refusal_reason_message`, and a new `datapath_translation` that carries out exactly the narrow `Outcome` match `translate_down` used to inline. `stored_c_path` was not named in the issue's move list but had to travel with `resolve_context_path`/`translated_read_component`, its only two call sites, both moving; keeping it in `resolve.rs` would have meant `path_conversion.rs` calling back into the interposition layer it's supposed to sit beneath. `TranslatedReadPath`/`ResolvedReadPath`'s fields are `pub(crate)` for one reason: `resolve.rs`'s read loop still turns each candidate into a `ReadAttempt` — a raw-pointer, C-touching construct `path_conversion.rs` deliberately does not know about — via an `impl TranslatedReadPath` block that stays in `resolve.rs` (Rust's orphan rule permits an inherent impl on a foreign same-crate type from anywhere in the crate). `resolve_arraystruct_argument`, `read_data_impl`, `ReadAttempt`, `validate_value_transformation`/`apply_value_transformation`, `live_conversion_record`, and the refusal/loss-log plumbing (`read_refusal`, `contextual_refusal`, `retain_read_fidelity(ies)`, `joined_argument_path`) all stay in `resolve.rs`: the read loop, value-transformation execution, and discovery/registration are issues #107–#109's scope, not this one. `concrete_stored_path`'s doc comment now states its two consumer-side narrowing rules explicitly — a structure path may carry neither an ordered candidate plan nor a value transformation — and why `conversion_map.rs` cannot state them itself: both depend on what the *consumer* can do with an `Outcome`, not on what the rule declares, since the same `merged` rule serves a read (which tries candidates) and would refuse a bare structure-path resolution on the identical outcome. `src/resolve.rs` falls 2129 → 1616 lines; `path_conversion.rs` is 582 lines including its own unit tests, which cover `resolve_read_path` (the not-found-for-an-unclaimed-path proof, issue #56's user story 47) and `join_hli_path`/`strip_anchor` — moved verbatim from `resolve.rs`'s test module, since they test only what moved. `resolve.rs` keeps the one remaining registry-related unit test, `a_data_path_seam_answers_before_the_registry_when_conversion_is_disabled`, since it tests `live_conversion_record`, which stays. `cargo test`'s 161 unit tests are unchanged in count. The C ABI suite is untouched — no assertion added, removed or edited, 166 tests with 17 real-core labelled, all green. A grep for `Outcome::` finds arms only in `path_conversion.rs` and `conversion_map.rs` within the shim's seam-policy code; `src/bin/validate_equilibrium_coverage.rs`, a separate coverage-reporting binary predating this issue (from the P2 review-fix batch) and untouched here, also matches on `Outcome` for its own unrelated purpose and is outside this series' scope.

**Update (issue #109, part E of the #101 series):** nine call sites in `src/interpose.rs` used to rebuild a `forward`/`end-on-refusal` closure pair differing only in which of two ABI symbol families they named — the ordinary HLI call or its `al_plugin_*` reentry twin. One `CallFamily` — a two-variant `Copy` enum, `Ordinary`/`Plugin` — now replaces every one of them, together with `ORDINARY`/`PLUGIN` associated constants that name the two values without holding a raw symbol pointer: `CallFamily` never resolves IMAS-Core itself, because several of the nine call sites refuse before ever forwarding at all (`begin_arraystruct_action_impl`'s argument resolution, `write_data_impl`'s mismatch check), and eagerly extracting a resolved fn pointer at construction time would attempt resolution on a path that used to skip it entirely. Six new dispatch functions — `call_begin_global`, `call_begin_slice`, `call_begin_arraystruct`, `call_read`, `call_write`, `call_end` — are the only six places a `CallFamily` value is ever matched against a real symbol name, each forwarding through the existing `forward_status!` macro exactly where the closure it replaces used to fire, so IMAS-Core resolution stays exactly as lazy as before. `begin_timerange_action` and `delete_data` take no `CallFamily` parameter at all: neither has a plugin twin (`al_plugin_begin_timerange_action` is unlinkable upstream; `al_plugin_delete_data` was never resolved), so there is no second symbol to choose between and no `Option<fn>` member is needed to paper over that gap. The six previously-duplicated seam-wrapper pairs collapse into a shared `*_seam`/`*_impl` function plus two one-line public wrappers each (`begin_global_action`/`plugin_begin_global_action` → `begin_global_action_seam`; likewise for slice, arraystruct, read, write and end), so `begin_arraystruct_action_impl` and `read_data_impl` — already shared since issues #67/#68 — change only how they forward, not their shape. `CallFamily`'s doc comment states the property the issue asks for and no more: a family mismatch requires bypassing the `family` parameter threaded through this module's policy functions, and a reviewer sees that bypass, because the six dispatch functions are the only place the symbol choice is made — not that a mismatch is unrepresentable, which it deliberately is not (the calling binary picks the family, not this type). `src/interpose.rs` grows 1453 → 1735 lines, the doc comments accounting for most of it. `cargo test`'s 165 unit tests, the 167-test real-Core ctest profile (17 real-core labelled) and the 146-test stub-only profile are all unchanged in count and green — no assertion added, removed or edited, since every call site's behavior and every refusal message is byte-for-byte what it was before.

Single crate at the repo root. Keep it that way until `imas-core-sys` lands — cargo allows only one package per `links` value, so the crate binding `libal` must be separate, and that is the moment to add `[workspace]` to `Cargo.toml` plus a `crates/` directory. Nothing moves when that happens.

**Language: Rust.** The C ABI artefacts (shared library, cbindgen-generated header, pkg-config file) are produced by [cargo-c]; CMake drives cargo-c rather than compiling anything itself. Toolchain on the ITER cluster comes from modules `Rust/1.88.0-GCCcore-14.3.0`, `cargo-c/0.10.15-GCCcore-14.3.0` and `IMAS-Core/5.7.1` — `source scripts/iter-env.sh`.

Real IMAS-Core is required by the default configure profile. CMake acquires it
in one of three modes (installed package lookup by default, development layout,
or download-and-build). CI's explicit `IMAS_MVDD_REAL_CORE_TESTS=OFF` profile
is the only stub-only path; it registers the recording-stub seams without
silently reducing the real-Core suite. See CMakeLists.txt's IMAS-Core
acquisition section for the option names and `IMAS_CORE_LIBRARY`-free test
wiring.

```console
$ cmake -S . -B build -DCMAKE_BUILD_TYPE=Release   # Debug → cargo `dev` profile
$ cmake --build build
$ ctest --test-dir build --output-on-failure       # rust-unit + abi-smoke + tracer (stub and real IMAS-Core)
$ cmake --install build --prefix /path/to/prefix
$ cargo fmt && cargo clippy --all-targets          # lint, no CMake wrapper
```

CI (`.github/workflows/ci.yml`) has a fast recording-stub job for fmt, clippy,
both CMake configurations, install and downstream consumption, plus a full job
on pull requests and `main` pushes that downloads and caches the pinned
IMAS-Core build before the drift and real-Core seams. It is the only thing
keeping the CMake path honest — `cargo test` alone never re-runs cargo-c, never
regenerates the header, and never compiles the C smoke test.

`README.md` carries the build options and layout. The *why* behind the build
lives in comments next to what it explains — `CMakeLists.txt` for the staging
tree, the install path, the multi-config refusal and the IMAS-Core
acquisition modes, `Cargo.toml` for the `capi` feature and the workspace
question. Keep it there rather than restating it in prose that can drift.

[cargo-c]: https://github.com/lu-zero/cargo-c

Adding a C ABI test: include `tests/shim_test_support.h` rather than writing a
prologue. It owns `CHECK`/`CHECK_OK`, IMAS-Core's four data-type codes, the
recording-stub accessors (`string_from_stub`, `int_from_stub`,
`double_from_stub`, `pointer_from_stub`, over one `open_recording_stub`),
`open_mismatched_occurrence`, and the `{name, function}` scenario table that
`RUN_NAMED_SCENARIO` dispatches `argv[1]` through. Register the scenario with
`add_stub_test(<ctest-name> <executable> <scenario> [HLI_DD_VERSION v]
[STAMP_VERSION v] [ENV "KNOB=value"...])`, which owns the shared environment.
Twelve copies of that prologue and a hundred inlined environment strings is
where they came from, and one of those copies printed `\n` as text in four
suites' failure messages for months; a new copy starts that over.


Reference documents:
- `docs/IMAS-CORE_FUNCTIONALITY_INVENTORY.md` — the primary technical reference (938 lines). A per-capability, code-verified inventory of the IMAS-Core surface this project must mirror. Read this before designing anything.
- `docs/PROTOTYPE_CRITIC.md` — critique of the earlier `dd-maps/` + `middleware/` prototype: which of its choices were load-bearing and which should not be inherited without a decision.
- `CODE_OF_CONDUCT.md` — ITER's Contributor Covenant; contact `imas-administration@iter.org`.

`IMAS-CORE_FUNCTIONALITY_INVENTORY.md` cross-references `NORTH_STAR.md`, `CONTEXT.md`, `CLAUDE.md`, `docs/adr/0001-*.md`, `CMakeLists.txt` and `src/**` paths. **Those live in the separate IMAS-Core repository, not here.** Every `src/...:NNN` and `include/...` citation in that document is a pointer into IMAS-Core's tree. Don't try to resolve them locally, and don't treat their absence as a gap in this repo.

## What this project is

A **shim between the IMAS HLIs and IMAS-Core**:

```
HLI (imas-Fortran, imas-CPP; imas-Matlab and imas-Java not yet judged)
        │  compiled/configured against DD version V
        ▼
IMAS-Multiversion-DD-Loader   ← this project: mirrors IMAS-Core's public C ABI
        │  translates DD paths V ⇄ W
        ▼
IMAS-Core (libal)             ← low-level access layer, stores IDSs written under DD version W
```

**imas-python is not a client** — it converts DD versions itself and holds one DD version per `DBEntry` rather than one per process, so the shim's version latch does not apply to it and stacking the two would convert twice. The criterion is the client's shape, not its language: any caller of the C ABI holding one DD version for the life of the process and not converting on its own is a client, whatever it is written in. See `docs/adr/0005-hli-dd-version-entry-point.md`.

The core idea: **this project re-exports IMAS-Core's public C ABI verbatim** — same function names, same signatures, same `al_status_t` contract — and interposes between the mirrored functions. An HLI set up for DD 4.1.1 can then read an IDS stored under an earlier DD by having its path arguments rewritten on the way down and results rewritten on the way back up.

Conversion is **best-effort and explicitly lossy**. Fields that were removed, renamed with changed semantics, or sign-flipped (COCOS) between versions cannot always round-trip. Loss must be surfaced, not silently swallowed — but note the ABI leaves little room for it: `al_status_t` carries a single `int code` plus a `char message[256]`, and `code == 0` means success. Deciding how partial/lossy conversions are reported through that narrow channel is a core design question, not an implementation detail.

## Architecture: where the conversion seams are

Derived from the inventory — these are the ABI entry points that carry DD paths or IDS names and therefore need translation. Everything else can pass straight through.

**Down-converted (HLI's DD version → stored DD version):**

| Function | Path-bearing arguments |
|---|---|
| `al_begin_global_action` | `dataobjectname` (IDS name), `datapath` |
| `al_begin_slice_action` | `dataobjectname` |
| `al_begin_timerange_action` | `dataobjectname` |
| `al_begin_arraystruct_action` | `path`, `timebase` |
| `al_read_data` / `al_write_data` | `field`, `timebase` |
| `al_delete_data` | `path` |
| `al_get_occurrences` | `ids_name` |
| `al_list_filled_paths` | `dataobjectname` |
| `al_bind_plugin` / `al_unbind_plugin` | `fieldPath` |
| `al_plugin_*` reentry family | same path arguments as their non-`al_plugin_` twins |

**Up-converted (stored → HLI's DD version):** `al_list_filled_paths`'s returned `path_list` is the main one — it hands back DD paths that were written under the stored version and must be presented in the caller's version. Note the caller owns and must free both the list and every string in it; a shim that rewrites those strings takes on that ownership contract too.

Also relevant to path handling: `field`/`path` arguments are **relative to `ctxID` unless prefixed with `/`**, in which case they're absolute. A converter must handle both forms, and must know the enclosing context's path to resolve the relative case — meaning the shim has to track context state (`dectxID`/`octxID`/`actxID` → resolved DD path), not just rewrite strings statelessly. AOS iteration via `al_iterate_over_arraystruct` mutates that state.

## Constraints inherited from the mirrored ABI

Read the inventory for the full picture; these are the ones that most directly shape this project's design.

- **`getDDVersion()` is deliberately dead in IMAS-Core** — it returns the sentinel `"!!DEPRECATED!!"`, and an upstream test asserts it stays that way. This project **cannot** ask IMAS-Core which DD version a pulse was written under via that call. Determining the stored version is an open problem and a prerequisite for conversion.
- **`datapath` on `al_begin_global_action` is near-inert.** HDF5, MDSplus, Memory, ASCII, and Flexbuffers all ignore it. Only UDA in remote mode with `cache_mode=ids` actually honors it. Don't build a partial-get strategy on it.
- **`al_list_filled_paths` hard-fails on 4 of 6 backends** (MDSplus, Memory, ASCII, Flexbuffers throw unconditionally; only HDF5 has a real implementation, UDA delegates). If the conversion logic wants to discover what's actually stored, that discovery path only works against HDF5/UDA. Plan a fallback.
- **Two conflicting meanings of `0` — but not at the shim.** In `al_status_t.code`, `0` = success. In `Backend::readData` (`al_backend.h:138`) / plugin `read_data`'s `int` return, `0` = *not found* and `1` = success. The shim never sees the second convention: all 37 mirrored symbols return `al_status_t`, both `int`-returning layers sit below the C ABI, and `al_register_plugin` takes a plugin *name* rather than callbacks, so the shim cannot become a plugin. What the shim does have to get right is the three-way read outcome — failure (`code != 0`), not-found (`code == 0` with a null data pointer), and data — which ADR 0012 confines to a single classifier function.
- **`MAXDIM = 7`** (max array rank), **`MAX_ERR_MSG_LEN = 256`** (`al_status_t.message`).
- **Data types: `CHAR_DATA`, `INTEGER_DATA`, `DOUBLE_DATA`, `COMPLEX_DATA` only** — no boolean, no single-precision float. DD type changes across versions must land in one of these four.
- **Time-range and slice support is not universal.** Only HDF5 supports `supportsTimeRangeOperation()` unconditionally; MDSplus supports interpolation but *not* time range; UDA's support is gated on the remote server plugin version (`> 1.4.0`); Memory/ASCII/Flexbuffers support neither.
- Several upstream behaviors are outright bugs or silent degradations the inventory documents in detail (e.g. `al_plugin_begin_timerange_action` has a header/impl signature mismatch and is unlinkable; `al_setvalue_*_parameter_plugin` null-derefs on an unregistered plugin name; `al_unregister_plugin` only destroys plugins that were bound). When mirroring the ABI, decide *deliberately* per case whether to reproduce the upstream behavior or fix it at the shim — and record the decision.

## Working with DD versions

Use the **`imas-dd` MCP server** as the authority on DD content and inter-version differences — do not guess at path renames or reason from memory about what changed between versions.

- 35 versions in the chain, `3.22.0` … `4.1.1`; `4.1.0` is flagged as current. The 3.x → 4.0.0 boundary is the big breaking one.
- `get_dd_migration_guide(from_version, to_version)` — breaking changes, COCOS sign-flip tables, path renames, unit changes. This is the closest thing to a specification of what the conversion layer must implement. Use `summary_only=true` / `ids_filter` to keep responses manageable; unfiltered full-DD guides are large.
- `get_dd_changelog` — ranks paths by volatility across versions; useful for finding where conversion will hurt most.
- `check_dd_paths` / `search_dd_paths` / `get_dd_version_context` — validate that a specific path exists in a specific version.
- `get_dd_cocos_fields` — COCOS-sensitive fields, i.e. the ones where conversion is a sign transformation, not a rename.

A rename table alone is insufficient for correctness: unit changes and COCOS sign flips are *value* transformations that have to happen on the data buffers in `al_read_data`/`al_write_data`, not on the path strings.

## Agent skills

### Issue tracker

Issues are tracked on GitHub (yohannmarguier/IMAS-Multiversion-DD-Loader), via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

Default label vocabulary: `needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context layout — `CONTEXT.md` + `docs/adr/` at the repo root, created lazily as terms/decisions get resolved. See `docs/agents/domain.md`.

## graphify

This project has a knowledge graph at graphify-out/ with god nodes, community structure, and cross-file relationships.

Rules:
- For codebase questions, first run `graphify query "<question>"` when graphify-out/graph.json exists. Use `graphify path "<A>" "<B>"` for relationships and `graphify explain "<concept>"` for focused concepts. These return a scoped subgraph, usually much smaller than GRAPH_REPORT.md or raw grep output.
- If graphify-out/wiki/index.md exists, use it for broad navigation instead of raw source browsing.
- Read graphify-out/GRAPH_REPORT.md only for broad architecture review or when query/path/explain do not surface enough context.
- After modifying code, run `graphify update .` to keep the graph current (AST-only, no API cost).
