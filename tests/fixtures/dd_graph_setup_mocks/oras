#!/usr/bin/env bash
set -euo pipefail

test "$1" = pull
shift
test "$1" = --output
output=$2
if test "${TEST_ORAS_FAIL:-}" = yes; then
    exit 23
fi
mkdir -p "$output"
cp "$TEST_GRAPH_ARCHIVE" "$output/imas-codex-graph-dd-v5.3.0.tar.gz"
printf '%s\n' "$*" >> "$TEST_ORAS_LOG"
